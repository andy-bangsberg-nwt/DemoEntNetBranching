@model Student

@{
    ViewData["Title"] = "View";
}

<h1>The Student View  *** Andy messed this up*****</h1>

<div class="table-responsive">
    <table class="table table-bordered table-hover">
        <thead>
            <tr>
                <th>ID</th>
                <th>First Name</th>
                <th>Last Name</th>
                <th>Image Path</th>
             
            </tr>
        </thead>
        <tbody>
            <tr>
                <td>@Model.ID</td>
                <td>@Model.FirstName</td>
                <td>@Model.LastName</td>
                <td>@Model.ImagePath</td>
                
            </tr>
        </tbody>
    </table>
    <p><img style="width:1000px; height:800px;" src="~/images/@Model.ImagePath" alt="@Model.ImagePath" /></p>
</div>
